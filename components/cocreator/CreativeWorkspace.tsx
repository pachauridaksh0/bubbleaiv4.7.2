import React from 'react';

// This component is deprecated and no longer in use.
// Each project type now has its own dedicated workspace component.
// This file is kept to avoid breaking changes in case of lingering imports,
// but it should be considered for removal in future refactoring.

export const CreativeWorkspace: React.FC = () => {
    return null;
};
